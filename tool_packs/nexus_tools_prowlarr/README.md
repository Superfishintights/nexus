# nexus_tools_prowlarr

Nexus tool pack for the Prowlarr API.

Configuration is read with `nexus.config.get_setting`:

- `PROWLARR_URL`
- `PROWLARR_API_KEY`

Install with:

```bash
pip install -e .
```

Then add this package root to `NEXUS_TOOL_PACKAGES`.
